============================================================
COMPLETE BUILD INSTRUCTIONS - COPY TO C:\pm\pulsemateconnect21
============================================================

PROJECT HAS BEEN COPIED TO: C:\pm\pulsemateconnect21

============================================================
METHOD 1: PowerShell Commands (FASTEST)
============================================================

1. Open PowerShell (Start → type "PowerShell")

2. Run these commands one by one:

cd C:\pm\pulsemateconnect21\android
.\gradlew clean
.\gradlew assembleRelease

3. Wait 5-10 minutes for build to complete

4. Install APK:

cd C:\pm\pulsemateconnect21
adb install android\app\build\outputs\apk\release\app-release.apk

============================================================
METHOD 2: Open in VS Code and Build
============================================================

1. Close current VS Code window

2. Open PowerShell and run:

code C:\pm\pulsemateconnect21

3. In VS Code terminal, run:

cd android
.\gradlew clean
.\gradlew assembleRelease

4. Install APK:

adb install android\app\build\outputs\apk\release\app-release.apk

============================================================
METHOD 3: Create Batch File and Run
============================================================

1. Create file: C:\pm\pulsemateconnect21\build.bat

2. Paste this content:

@echo off
cd /d C:\pm\pulsemateconnect21\android
call gradlew clean
call gradlew assembleRelease
pause

3. Double-click build.bat to run

============================================================
EXPECTED OUTPUT LOCATION
============================================================

C:\pm\pulsemateconnect21\android\app\build\outputs\apk\release\app-release.apk

============================================================
TROUBLESHOOTING
============================================================

If build fails:
- Make sure you're in C:\pm\pulsemateconnect21 (NOT old location)
- Check Node.js is installed: node --version
- Check Java is installed: java -version
- Try: .\gradlew clean --refresh-dependencies

If "gradlew not found":
- Make sure you're in the android folder
- Try: .\gradlew.bat instead of .\gradlew

============================================================
AFTER SUCCESSFUL BUILD
============================================================

1. Install APK on device/emulator:
   adb install android\app\build\outputs\apk\release\app-release.apk

2. Open app - it will crash (Firebase not configured yet)

3. Configure Firebase Console (see FIREBASE-CONSOLE-SETUP.md)

4. Rebuild and test again

============================================================
QUICK COMMANDS SUMMARY
============================================================

# Navigate to project
cd C:\pm\pulsemateconnect21

# Build APK
cd android
.\gradlew assembleRelease

# Install APK
adb install android\app\build\outputs\apk\release\app-release.apk

# Check if device connected
adb devices

# Uninstall old version first (if needed)
adb uninstall in.pulsemateconnect.patient

============================================================
